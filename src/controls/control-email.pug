block controlBlock
  input.form-control(
    id="control-" + control.backField.name
    type="email"
    name=control.formName
    ng-model=control.model
  )&attributes(control.frontField.attributes)
